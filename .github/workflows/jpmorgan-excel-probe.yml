name: JPMorgan Excel probe

on:
  pull_request:
    branches:
      - feature/jpmorgan-00401a-scraper

permissions:
  contents: write

jobs:
  diagnose-full-suite:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: refactor/jpmorgan-excel-endpoint
          path: repo
          fetch-depth: 1
          show-progress: false
      - uses: actions/checkout@v4
        with:
          ref: probe/jpmorgan-excel-endpoint-run
          path: output
          fetch-depth: 1
          show-progress: false
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Capture full suite
        working-directory: repo
        shell: bash
        run: |
          python -m pip install -q -r requirements.txt
          set +e
          PYTHONPATH=scripts pytest -q --tb=short > /tmp/full-suite.log 2>&1
          status=$?
          cp /tmp/full-suite.log ../output/full-suite-error.txt
          cd ../output
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add full-suite-error.txt
          git commit -m 'chore: capture JPMorgan full-suite diagnostic' || true
          git push origin HEAD:probe/jpmorgan-excel-endpoint-run
          exit $status
