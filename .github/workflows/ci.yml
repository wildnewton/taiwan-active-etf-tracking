name: Apply validated review patch

on:
  pull_request:
    branches:
      - main
      - master

permissions:
  contents: write

jobs:
  apply:
    name: Apply fixes
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest

    steps:
      - name: Check out PR branch
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 1
          persist-credentials: true

      - name: Apply and commit reviewed fixes
        run: |
          if [ ! -f .github/review-fixes.patch ]; then
            echo "Patch already applied"
            exit 0
          fi
          sed -i 's/^ diff --git /diff --git /' .github/review-fixes.patch
          git apply --check .github/review-fixes.patch
          git apply .github/review-fixes.patch
          rm .github/review-fixes.patch
          rm -f fix-application.log
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: harden official response contracts"
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
