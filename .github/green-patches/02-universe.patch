diff --git a/scripts/etf_universe.py b/scripts/etf_universe.py
index 1413b22..607c64e 100644
--- a/scripts/etf_universe.py
+++ b/scripts/etf_universe.py
@@ -56,6 +56,20 @@ def _is_scope_excluded(row: dict) -> bool:
     return any(marker in official_logic for marker in _SCOPE_EXCLUSION_MARKERS)
 
 
+def _is_listed_on(row: dict, as_of_date: str) -> bool:
+    listing_date = row.get("listing_date")
+    return listing_date is None or listing_date <= as_of_date
+
+
+def _is_eligible_on(row: dict, as_of_date: str) -> bool:
+    if _is_scope_excluded(row) or not _is_listed_on(row, as_of_date):
+        return False
+    if not row.get("retired"):
+        return True
+    last_active_date = row.get("last_active_date")
+    return last_active_date is not None and as_of_date <= last_active_date
+
+
 def _with_derived_fields(row: dict) -> dict:
     return {**row, "moneydj_url": get_moneydj_url(row["code"])}
 
@@ -89,8 +103,6 @@ def _count_rows() -> int:
 def _last_active_from_row(row: dict) -> str | None:
     if row.get("last_active_date") is not None:
         return row.get("last_active_date")
-    if row.get("retired") and row.get("retired_since") is not None:
-        return row.get("retired_since")
     return row.get("last_seen_date") or row.get("retired_since")
 
 
@@ -118,6 +130,7 @@ def seed_etf_universe_from_file(path: str | Path | None = None, seen_date: str |
     with db._connect() as conn:
         for raw in rows:
             code = raw["code"].upper()
+            last_active_date = seen_date if _is_listed_on(raw, seen_date) else None
             existing = conn.execute(
                 "SELECT 1 FROM etf_universe WHERE code = ?",
                 (code,),
@@ -142,7 +155,7 @@ def seed_etf_universe_from_file(path: str | Path | None = None, seen_date: str |
                     raw.get("isin"),
                     raw.get("listing_date"),
                     seen_date,
-                    seen_date,
+                    last_active_date,
                     raw.get("official_url"),
                     raw.get("official_method"),
                     raw.get("official_logic"),
@@ -180,28 +193,39 @@ def get_active_etfs(as_of_date: date | datetime | str | None = None) -> list[dic
         ).fetchall()
     finally:
         conn.row_factory = old
-    return [_with_derived_fields(row) for row in rows]
+    return [
+        _with_derived_fields(row)
+        for row in rows
+        if not _is_scope_excluded(row)
+    ]
 
 
 def get_active_etf_count(as_of_date: date | datetime | str | None = None) -> int:
+    return len(get_active_etfs(as_of_date))
+
+
+def get_eligible_etf_codes(
+    as_of_date: date | datetime | str,
+) -> list[str]:
+    """Return ETFs that belonged to the analysis universe on one date."""
     ensure_seeded()
     as_of_date = _as_date_text(as_of_date)
     conn = db._connect()
     old = conn.row_factory
-    conn.row_factory = None
+    conn.row_factory = _dict_factory
     try:
-        row = conn.execute(
-            """
-            SELECT COUNT(*)
-            FROM etf_universe
-            WHERE retired = 0
-              AND (listing_date IS NULL OR listing_date <= ?)
-            """,
-            (as_of_date,),
-        ).fetchone()
+        rows = conn.execute(
+            "SELECT * FROM etf_universe ORDER BY code"
+        ).fetchall()
     finally:
         conn.row_factory = old
-    return row[0] if row else 0
+    return [row["code"] for row in rows if _is_eligible_on(row, as_of_date)]
+
+
+def get_eligible_etf_count(
+    as_of_date: date | datetime | str,
+) -> int:
+    return len(get_eligible_etf_codes(as_of_date))
 
 
 def get_etf_config(code: str) -> dict:
@@ -279,7 +303,7 @@ def retire_etf(
     retired_since: str | None = None,
 ) -> None:
     ensure_seeded()
-    last_active_date = last_active_date or retired_since or _today()
+    last_active_date = last_active_date or retired_since
     now = _now()
     with db._connect() as conn:
         conn.execute(
@@ -295,22 +319,6 @@ def retire_etf(
         )
 
 
-def _reactivate_etf(code: str, seen_date: str) -> None:
-    now = _now()
-    with db._connect() as conn:
-        conn.execute(
-            """
-            UPDATE etf_universe
-            SET retired = 0,
-                pending_retirement_since = NULL,
-                last_active_date = ?,
-                updated_at = ?
-            WHERE code = ?
-            """,
-            (seen_date, now, code.upper()),
-        )
-
-
 def reconcile_discovered_universe(
     discovered_rows: list[dict],
     seen_date: str | None = None,
@@ -337,7 +345,8 @@ def reconcile_discovered_universe(
     with db._connect() as conn:
         current_rows = conn.execute(
             """
-            SELECT code, retired, pending_retirement_since, official_logic
+            SELECT code, retired, pending_retirement_since, official_logic,
+                   listing_date
             FROM etf_universe
             """
         ).fetchall()
@@ -346,11 +355,19 @@ def reconcile_discovered_universe(
                 "retired": row[1],
                 "pending_retirement_since": row[2],
                 "official_logic": row[3],
+                "listing_date": row[4],
             }
             for row in current_rows
         }
 
         for code, raw in sorted(discovered.items()):
+            existing_listing_date = known.get(code, {}).get("listing_date")
+            effective_listing_date = raw.get("listing_date") or existing_listing_date
+            observed_active_date = (
+                seen_date
+                if effective_listing_date is None or effective_listing_date <= seen_date
+                else None
+            )
             if code not in known:
                 conn.execute(
                     """
@@ -370,7 +387,7 @@ def reconcile_discovered_universe(
                         raw.get("isin"),
                         raw.get("listing_date"),
                         seen_date,
-                        seen_date,
+                        observed_active_date,
                         now,
                         now,
                     ),
@@ -404,7 +421,7 @@ def reconcile_discovered_universe(
                     UPDATE etf_universe
                     SET retired = 0,
                         pending_retirement_since = NULL,
-                        last_active_date = ?,
+                        last_active_date = COALESCE(?, last_active_date),
                         market = COALESCE(?, market),
                         isin = COALESCE(?, isin),
                         listing_date = COALESCE(?, listing_date),
@@ -412,7 +429,7 @@ def reconcile_discovered_universe(
                     WHERE code = ?
                     """,
                     (
-                        seen_date,
+                        observed_active_date,
                         raw.get("market"),
                         raw.get("isin"),
                         raw.get("listing_date"),
@@ -425,7 +442,7 @@ def reconcile_discovered_universe(
                 conn.execute(
                     """
                     UPDATE etf_universe
-                    SET last_active_date = ?,
+                    SET last_active_date = COALESCE(?, last_active_date),
                         pending_retirement_since = NULL,
                         market = COALESCE(?, market),
                         isin = COALESCE(?, isin),
@@ -434,7 +451,7 @@ def reconcile_discovered_universe(
                     WHERE code = ?
                     """,
                     (
-                        seen_date,
+                        observed_active_date,
                         raw.get("market"),
                         raw.get("isin"),
                         raw.get("listing_date"),
@@ -459,12 +476,11 @@ def reconcile_discovered_universe(
                     """
                     UPDATE etf_universe
                     SET retired = 1,
-                        last_active_date = ?,
                         pending_retirement_since = NULL,
                         updated_at = ?
                     WHERE code = ?
                     """,
-                    (seen_date, now, code),
+                    (now, code),
                 )
                 retired.append(code)
             else:
