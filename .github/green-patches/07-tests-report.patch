diff --git a/tests/test_report_canonical_sources.py b/tests/test_report_canonical_sources.py
index 325001b..f761efe 100644
--- a/tests/test_report_canonical_sources.py
+++ b/tests/test_report_canonical_sources.py
@@ -40,6 +40,29 @@ def insert_holding(
                 f"{date}T00:00:00",
             ),
         )
+        stock_total = conn.execute(
+            """
+            SELECT COALESCE(SUM(weight_pct), 0.0)
+            FROM etf_daily_holdings
+            WHERE date = ? AND etf_code = ? AND source_type = ?
+            """,
+            (date, etf_code, source_type),
+        ).fetchone()[0]
+        conn.execute(
+            """
+            INSERT OR REPLACE INTO etf_daily_non_stock_assets (
+                date, etf_code, asset_name, asset_type, weight_pct,
+                source_url, source_type, extraction_method, scraped_at
+            ) VALUES (?, ?, '現金', 'cash', ?, 'https://test', ?, 'test', ?)
+            """,
+            (
+                date,
+                etf_code,
+                100.0 - stock_total,
+                source_type,
+                f"{date}T00:00:00",
+            ),
+        )
 
 
 def insert_full_day(date, canonical_weight=90.0, duplicate_weight=1.0):
diff --git a/tests/test_report_change_diagnostics.py b/tests/test_report_change_diagnostics.py
index b71fb9d..61bde8d 100644
--- a/tests/test_report_change_diagnostics.py
+++ b/tests/test_report_change_diagnostics.py
@@ -10,7 +10,7 @@ ETF_CODES = [
 ]
 
 
-def insert_holding(date, etf_code, weight_pct=90.0):
+def insert_holding(date, etf_code, weight_pct=100.0):
     with db._connect() as conn:
         conn.execute(
             """
diff --git a/tests/test_report_redesign.py b/tests/test_report_redesign.py
index 769671d..5b61f6b 100644
--- a/tests/test_report_redesign.py
+++ b/tests/test_report_redesign.py
@@ -24,6 +24,16 @@ def insert_holding(date, etf_code, stock_code="2330", stock_name="台積電", we
             """,
             (date, etf_code, f"{stock_name}({stock_code}.TW)", stock_code, stock_name, weight_pct, f"{date}T00:00:00"),
         )
+        conn.execute(
+            """
+            INSERT OR REPLACE INTO etf_daily_non_stock_assets (
+                date, etf_code, asset_name, asset_type, weight_pct,
+                source_url, source_type, extraction_method, scraped_at
+            ) VALUES (?, ?, '現金', 'cash', ?, 'https://test',
+                      'moneydj_primary', 'test', ?)
+            """,
+            (date, etf_code, 100.0 - weight_pct, f"{date}T00:00:00"),
+        )
 
 
 def ensure_signal_table():
diff --git a/tests/test_retry_stale_scrapes.py b/tests/test_retry_stale_scrapes.py
index 2bf8cbc..a4591af 100644
--- a/tests/test_retry_stale_scrapes.py
+++ b/tests/test_retry_stale_scrapes.py
@@ -31,7 +31,7 @@ def _holding(data_date, etf_code):
             stock_code="2330",
             stock_name="Stock 2330",
             shares=100,
-            weight_pct=10,
+            weight_pct=100,
             source_url="https://example.com",
             source_type="moneydj_primary",
             extraction_method="test",
diff --git a/tests/test_signal_report.py b/tests/test_signal_report.py
index 3da5fe6..37e008b 100644
--- a/tests/test_signal_report.py
+++ b/tests/test_signal_report.py
@@ -104,6 +104,23 @@ def insert_holding(date, etf_code, stock_code, stock_name, weight_pct=5.0):
             """,
             (date, etf_code, f"{stock_name}({stock_code}.TW)", stock_code, stock_name, weight_pct, f"{date}T00:00:00"),
         )
+        stock_total = conn.execute(
+            """
+            SELECT COALESCE(SUM(weight_pct), 0.0)
+            FROM etf_daily_holdings
+            WHERE date = ? AND etf_code = ? AND source_type = 'test'
+            """,
+            (date, etf_code),
+        ).fetchone()[0]
+        conn.execute(
+            """
+            INSERT OR REPLACE INTO etf_daily_non_stock_assets (
+                date, etf_code, asset_name, asset_type, weight_pct,
+                source_url, source_type, extraction_method, scraped_at
+            ) VALUES (?, ?, '現金', 'cash', ?, 'https://test', 'test', 'test', ?)
+            """,
+            (date, etf_code, 100.0 - stock_total, f"{date}T00:00:00"),
+        )
 
 
 def test_get_latest_signal_date_returns_most_recent_date():
diff --git a/tests/test_skip_stale_existing_snapshots.py b/tests/test_skip_stale_existing_snapshots.py
index 1d88823..5bc8698 100644
--- a/tests/test_skip_stale_existing_snapshots.py
+++ b/tests/test_skip_stale_existing_snapshots.py
@@ -112,7 +112,7 @@ def test_snapshot_exists_detects_existing_stock_snapshot():
             stock_code="2330",
             stock_name="台積電",
             shares=1000,
-            weight_pct=10.0,
+            weight_pct=100.0,
             source_url="https://example.test",
             source_type="moneydj_primary",
             extraction_method="requests_bs4",
diff --git a/tests/test_try_run_preexisting_success.py b/tests/test_try_run_preexisting_success.py
index 6c56b1f..1aa8915 100644
--- a/tests/test_try_run_preexisting_success.py
+++ b/tests/test_try_run_preexisting_success.py
@@ -41,7 +41,7 @@ def _seed_validated_snapshot(db_path: Path):
             stock_code="2330",
             stock_name="台積電",
             shares=1000,
-            weight_pct=10.0,
+            weight_pct=100.0,
             source_url="https://example.test",
             source_type="moneydj_primary",
             extraction_method="test",
